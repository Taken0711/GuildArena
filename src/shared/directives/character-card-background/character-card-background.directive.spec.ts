import { CharacterCardBackgroundDirective } from './character-card-background.directive';

describe('CharacterCardBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new CharacterCardBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
