import { SpellIconDirective } from './spell-icon.directive';

describe('SpellIconDirective', () => {
  it('should create an instance', () => {
    const directive = new SpellIconDirective();
    expect(directive).toBeTruthy();
  });
});
